<% content_for :head do %>
  <%= image_tag('header-image.jpg', id: 'content-header-img', alt: 'Check Out our Courses') %>
<% end %>

  <div class="container">

        <!-- Jumbotron Header -->
        <!-- <header class="jumbotron hero-spacer">
            <h1>A Warm Welcome!</h1>
            <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Ipsa, ipsam, eligendi, in quo sunt possimus non incidunt odit vero aliquid similique quaerat nam nobis illo aspernatur vitae fugiat numquam repellat.</p>
            <p><a class="btn btn-primary btn-large">Call to action!</a>
            </p>
        </header>

        <hr> -->

        <!-- Title -->
        <div class="row">
          <div class="col-lg-12">
              <h3>Latest Features</h3>
          </div>
        </div>
        <!-- /.row -->

        <!-- Page Features -->
        <div class="transitions-enabled" id="products">
          <% @products.each do |product| %>
            <div class="box panel panel-default"><a href="<%= product_path(product) %>">
              <img src="<%= asset_path product.thumbnail_image_name %>" alt="<%= product.title %>"/>
              <div class="panel-body">
                <h2>
                  <p class="product-title"><%= product.title %></p>
                </h2>
                <span class="product-price">$<%= product.price %></span>
              </div>
            </div>
          <% end %>
        </div>

<!-- <section id="products" class="transition-enabled">
  <article id="details-article" class="col-sm-12">
    <h1 id="article-title">Our Courses<span class="font-weight-light"></span></h1>
  </article>
  <% @products.each do |product| %>
    <div class="product clearfix">
      <a href="<%= product_path(product) %>">
        <div class="product-image-container">
          <img src="<%= asset_path product.thumbnail_image_name %>" alt="<%= product.title %>"/>
          <span class="product-price">$<%= product.price %></span>
        </div>
        <p class="product-title"><%= product.title %></p>
      </a>
    </div>
  <%end%>
 </section> -->